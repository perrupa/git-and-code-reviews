# Aliases

```
echo "[alias]
	co = checkout
	ci = commit
	st = status
	br = branch
	pr = pull --rebase
	hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
	type = cat-file -t
	dump = "cat-file -p"
	logo = log --oneline
	lg= log --graph --decorate --oneline" > ~/.gitconfig 
```
